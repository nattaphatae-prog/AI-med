# 🏥 AI Medical Assistant (ผู้ช่วยแพทย์อัจฉริยะ)

> **โปรเจกต์นี้นำเสนอการผสานรวมของปัญญาประดิษฐ์เชิงสร้างสรรค์ (Generative AI) และ AI ด้านภาพ (Computer Vision) เพื่อสร้างเครื่องมือสนับสนุนการตัดสินใจทางการแพทย์แบบครบวงจรสำหรับบุคลากรทางการแพทย์**

## 🌟 จุดเด่นของผลงาน (Project Highlights)

โปรเจกต์นี้ได้รับการพัฒนาขึ้นโดยใช้สถาปัตยกรรม **Full-Stack** ที่ประกอบด้วย **FastAPI** (Python) เป็น Backend และ **HTML/Tailwind CSS/JavaScript** เป็น Frontend โดยมีหัวใจหลักคือโมเดล AI ระดับโลก:

| คุณสมบัติ | เทคโนโลยีหลัก | บทบาททางการแพทย์ |
| :--- | :--- | :--- |
| **AI Medical Chat** | **Google Gemini 2.0 Flash** | ให้คำปรึกษาทางการแพทย์เบื้องต้น, อธิบายอาการ/โรค, และตอบคำถามสุขภาพด้วยความรวดเร็วและเป็นมิตร |
| **Image Analysis** | **YOLOv8 (Computer Vision)** | วิเคราะห์ภาพทางการแพทย์ (X-ray, Blood Smear) เพื่อช่วยในการจำแนกโรคและนับจำนวนเซลล์ |
| **Patient Data Management** | **FastAPI + Pandas** | จัดการและดึงข้อมูลสถิติผู้ป่วย, ระบบค้นหาข้อมูลผู้ป่วยแบบเรียลไทม์ (จำลอง) |

---

## 👩‍⚕️ ผู้จัดทำ (Developer)

| รายละเอียด | ข้อมูล |
| **ชื่อ:** | ณัฏฐพัฒน์ จุลพันธ์ |
| **สถาบัน:** | สาธิตเกษตร |
| **สาขา/ความสนใจ:** | สัตวแพทย์/แพทย์ |

> *ในฐานะผู้ที่มีความสนใจในสายงานด้านการแพทย์และวิทยาการคอมพิวเตอร์อย่างลึกซึ้ง โปรเจกต์นี้เป็นการนำความรู้ด้าน AI มาประยุกต์ใช้เพื่อเพิ่มประสิทธิภาพและลดภาระงานของบุคลากรทางการแพทย์ในอนาคต*

---

## 🛠️ สถาปัตยกรรมและเทคโนโลยีที่ใช้ (Architecture & Tech Stack)

### Backend (FastAPI)

* **ภาษา:** Python
* **Web Framework:** FastAPI (API Service ที่รวดเร็วและมีประสิทธิภาพสูง)
* **Generative AI:** `google-generativeai` (ควบคุมโมเดล **Gemini 2.0 Flash**)
    * **การจัดการ Session:** ใช้การเก็บ `chat_sessions` (in-memory) เพื่อให้ AI จดจำประวัติการสนทนาของผู้ใช้แต่ละราย
* **Computer Vision (CV):** `ultralytics` (สำหรับโมเดล **YOLOv8** ที่ถูกเทรนล่วงหน้าแล้ว)
    * **การวิเคราะห์ภาพ:** รองรับการวิเคราะห์ภาพ 2 ประเภท คือ X-ray (Classification) และ Blood Smear (Object Detection)
* **Data Handling:** `pandas` (สำหรับการจัดการและประมวลผลข้อมูลผู้ป่วย `healthcare_dataset.csv`)

### Frontend (SPA - Single Page Application)

* **Markup:** HTML
* **Styling:** Tailwind CSS (เพื่อความรวดเร็วในการพัฒนาและดีไซน์ที่ทันสมัย)
* **Interactivity:** JavaScript (Vanilla JS)
* **Icons:** Lucide Icons (สำหรับไอคอนทางการแพทย์และ UI)
* **User Interface:** ออกแบบเป็น Responsive Web Application รองรับการใช้งานทั้งบน Desktop และ Mobile

---

## 💡 วิธีการทำงานของ Feature หลัก

### 1. AI Medical Chat (`/chat`)

* **Session Management:** เมื่อผู้ใช้เริ่มต้นแชท, Frontend จะส่ง `session_id` ที่สร้างขึ้นด้วย `crypto.randomUUID()` ไปยัง Backend
* **System Instruction:** Backend กำหนด `system_instruction` ที่ชัดเจนให้กับ Gemini โดยระบุบทบาทเป็น **'AI ผู้ช่วยแพทย์'** เน้นการตอบคำถาม, อธิบายอาการ/โรค, และการให้คำแนะนำเบื้องต้น
* **Constraints:** กำหนดให้คำตอบ **สั้นกระชับ ไม่เกิน 100 คำ** และ **เตือนให้ปรึกษาแพทย์เสมอ**
* **Model Config:** ใช้ `gemini-2.0-flash-exp` ที่มี `temperature=0.7` และ `max_output_tokens=500` เพื่อให้ได้คำตอบที่สมดุลระหว่างความแม่นยำและความเป็นธรรมชาติ

### 2. Image Analysis (`/analyze`)

1.  ผู้ใช้อัปโหลดภาพและเลือกประเภท (`xray` หรือ `blood`)
2.  Backend รับไฟล์และประเภทภาพ
3.  **ถ้า `image_type` เป็น `xray`:**
    * ใช้โมเดล `xray_best.pt` (Classification Model)
    * ส่งคืนผลลัพธ์หลัก (`result`) และระดับความมั่นใจ (`confidence`)
4.  **ถ้า `image_type` เป็น `blood`:**
    * ใช้โมเดล `blood_best.pt` (Detection Model)
    * ส่งคืนจำนวนเซลล์รวม (`total_cells`) และการนับเซลล์แต่ละประเภท (`cell_counts`)

### 3. Patient Search (`/patient/search`)

* API นี้อนุญาตให้ค้นหาข้อมูลผู้ป่วย (จำลอง) ตามเงื่อนไข 3 ประเภท: **ชื่อ (`name`)**, **อาการ/โรค (`condition`)**, และ **กรุ๊ปเลือด (`blood_type`)**
* ใช้คำสั่ง `pandas.DataFrame.str.contains` สำหรับการค้นหาข้อความ และจำกัดผลลัพธ์ไว้ที่ 10 รายการแรก

---

## 🚀 การติดตั้งและรันโปรเจกต์ (Setup & Run)

*(เพิ่มคำแนะนำการติดตั้ง Environment, การตั้งค่า `GEMINI_API_KEY` และคำสั่งรัน Uvicorn ในส่วนนี้)*
